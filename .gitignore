# Environment variables (rahasia, jangan di-commit)
.env
.env.local

# CATATAN: firebase-config.js HARUS ikut di-commit dan di-deploy
# karena ini adalah client-side config yang memang dirancang untuk publik.
# Keamanan data diatur lewat Firebase Security Rules, bukan menyembunyikan API key.

# OS files
.DS_Store
Thumbs.db
